/** Ids of headings the page takes the person to. */

export const RESULTS_TITLE_ID = "results-title";

/** The id of a route's heading in the result. */
export const routeResultId = (routeId: string) => `result-${routeId}`;
