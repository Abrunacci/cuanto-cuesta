# cuanto-cuesta backend

The researched fee defaults (`config/fees.yaml`) and the code that loads and checks them. The
scheduled jobs and the FastAPI service of stages 2 and 3 build on it. See `CONTRIBUTING.md` at the
repo root for the architecture and conventions.
